import 'package:flutter/material.dart';

abstract final class AppColors {
  static const background = Color(0xFF07090F);
    static const surface = Color(0xFF0E111A);
      static const surfaceElevated = Color(0xFF151925);

        static const primary = Color(0xFF7C5CFF);
          static const secondary = Color(0xFF35C8FF);

            static const textPrimary = Color(0xFFF5F7FF);
              static const textSecondary = Color(0xFF9AA3B5);
                static const textMuted = Color(0xFF626B7D);

                  static const border = Color(0x1FFFFFFF);
                    static const success = Color(0xFF35D07F);
                      static const error = Color(0xFFFF5C72);

                        static const transparent = Colors.transparent;
                        }